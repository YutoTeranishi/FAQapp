
  <% if answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question.errors.count, "error") %> prohibited this question from being saved:</h2>
      <ul>
        <% question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= form_for(answer) do |form| %>
    <%= form.hidden_field :question_id, {value:answer.question_id} %>
    <%= form.hidden_field :person_id,{value:current_account.id}%>
  
  <div class="form-grop">
    <%= form.label :content %>
    <%= form.text_area :content, rows:2, class:"form-control"%>
  </div>

  <%= form.hidden_field :finished,{value:false} %>

    <%= form.submit %>

<% end %>
